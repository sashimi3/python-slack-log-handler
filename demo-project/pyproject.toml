[project]
name = "demo-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "slack-log-handler>=0.3.0",
]

[tool.uv.sources]
slack-log-handler = { path = "../", editable = true }
